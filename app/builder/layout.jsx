import '@/app/_styles/builder.css';

export const metadata = {
  title: 'Builder - Blockify Builder',
  description: 'Drag-and-drop Shopify page builder',
};

export default function BuilderLayout({ children }) {
  return children;
}
